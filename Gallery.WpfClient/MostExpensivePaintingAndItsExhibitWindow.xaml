<Window x:Class="Gallery.WpfClient.MostExpensivePaintingAndItsExhibitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gallery.WpfClient"
        mc:Ignorable="d"
        Title="MostExpensivePaintingAndItsExhibitWindow" Height="300" Width="450">
    <Grid>
        <Grid>
            <DataGrid x:Name="dataGrid" ItemsSource="{Binding MostExpensivePaintingAndItsExhibit}" AutoGenerateColumns="True" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            </DataGrid>
        </Grid>
    </Grid>
</Window>
